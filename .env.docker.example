# Copie ce fichier vers .env.docker si tu veux personnaliser Docker Compose.
# Les vraies valeurs sensibles sont dans ./secrets/*.txt, pas ici.

APP_ENV=production
FRONTEND_PORT=8080
BACKEND_PORT=8000
FRONTEND_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

DB_NAME=staelle_market
DB_USER=staelle
POINTS_PER_XAF=1000
